# OS
.DS_Store

# Personal data (PRIVATE — never commit)
# Raw note snapshots
reflect-*/
# Reflection index with personal goals/summaries
index/
!index/.gitkeep
# Coaching session outputs
reflections/
!reflections/.gitkeep
# Local MCP server URL
.mcp.json

# Local tool config (symlinks to user's gstack installation)
.claude/skills/

# Build state
*.build-state.json
